<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.quizapp.system.mapper.SubjectMapper">

  <resultMap id="SubjectResult" type="com.quizapp.system.domain.Subject">
    <id property="id" column="id"/>
    <result property="name" column="name"/>
    <result property="sortOrder" column="sort_order"/>
    <result property="createTime" column="create_time"/>
    <result property="updateTime" column="update_time"/>
  </resultMap>

  <select id="selectAll" resultMap="SubjectResult">
    SELECT id, name, sort_order, create_time, update_time
    FROM subject
    ORDER BY sort_order
  </select>

  <select id="selectById" parameterType="long" resultMap="SubjectResult">
    SELECT id, name, sort_order, create_time, update_time
    FROM subject
    WHERE id = #{id}
  </select>
</mapper>
